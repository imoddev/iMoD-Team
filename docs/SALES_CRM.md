# 💼 Sales CRM & Report Module

## ภาพรวม

ระบบ CRM สำหรับทีมขาย Mod Media เน้นการจัดการลูกค้า ติดตามสถานะงาน
และสร้างรายงาน Performance ของคอนเทนต์ที่โพสต์ในโซเชียลมีเดียให้ลูกค้า

---

## 📊 Features

### 1. Client Management (จัดการลูกค้า)

**ข้อมูลที่เก็บ:**
- ชื่อบริษัท / แบรนด์
- ผู้ติดต่อ (ชื่อ, ตำแหน่ง, เบอร์, อีเมล, LINE)
- หมวดหมู่ธุรกิจ (Tech, Automotive, Finance, Lifestyle, etc.)
- งบประมาณ / Package ที่ใช้
- ประวัติการทำงานร่วมกัน
- หมายเหตุ / บันทึกการประชุม

**ฟีเจอร์:**
- ค้นหา / กรอง ลูกค้าตามหมวดหมู่, สถานะ, งบ
- Import/Export ข้อมูลลูกค้า (CSV/Excel)
- ดูประวัติ Campaign ทั้งหมดของลูกค้า

### 2. Sales Pipeline (สถานะงาน)

```
Lead → เสนอราคา → ตกลง → กำลังทำ → ส่งมอบ → ปิดงาน
 │                                              │
 └──── (ไม่ตกลง) ──── Archive ─────────────────┘
```

**แต่ละสถานะเก็บ:**
- วันที่เปลี่ยนสถานะ
- ผู้รับผิดชอบ
- มูลค่างาน
- รายละเอียด / เงื่อนไข
- ไฟล์แนบ (Quotation, Brief, Contract)

**มุมมอง:**
- Kanban Board (ลากย้ายสถานะได้)
- List View (ตารางกรอง/เรียงได้)
- Calendar View (ดู Deadline, Follow-up)

### 3. Content Performance Tracking (ติดตามสถิติ)

**Social Platforms ที่เชื่อม:**
- Facebook Page → Insights API
- YouTube → Analytics API
- X (Twitter) → Analytics
- TikTok → Analytics
- Instagram → Insights API

**Metrics ที่เก็บ:**
| Metric | คำอธิบาย |
|--------|----------|
| Reach | จำนวนคนเห็น |
| Impressions | จำนวนครั้งที่แสดง |
| Engagement | Like + Comment + Share |
| Engagement Rate | Engagement / Reach × 100 |
| Views | ยอดวิว (วิดีโอ) |
| Clicks | คลิกลิงก์ |
| Watch Time | ระยะเวลาดู (วิดีโอ) |

**ฟีเจอร์:**
- เลือกช่วงเวลา (7 วัน, 30 วัน, Custom)
- เปรียบเทียบ Performance ข้ามโพสต์ / ข้ามแคมเปญ
- กราฟ Trend (เส้น, แท่ง, วงกลม)
- Top Performing Content

### 4. Auto-generate Client Report (สร้างรายงานอัตโนมัติ)

**รายงานประกอบด้วย:**
- ข้อมูลแคมเปญ (ชื่อ, ช่วงเวลา, Platform)
- สรุป Performance รวม
- รายละเอียดแต่ละโพสต์ พร้อม Screenshot
- กราฟเปรียบเทียบ
- ข้อเสนอแนะ / Insights

**Output Format:**
- PDF (ส่งลูกค้า)
- Google Slides (Presentation)
- Dashboard Link (ลูกค้าดูเองได้)

---

## 🗄️ Database Tables

```sql
-- ลูกค้า
clients (
  id, company_name, brand, contact_name, contact_phone,
  contact_email, contact_line, category, budget_range,
  notes, created_at, updated_at
)

-- แคมเปญ/งาน
campaigns (
  id, client_id, name, description, status,
  value, start_date, end_date, assigned_to,
  created_at, updated_at
)

-- โพสต์ในแคมเปญ
campaign_posts (
  id, campaign_id, platform, post_url, post_date,
  content_type, reach, impressions, engagement,
  views, clicks, watch_time, screenshot_url,
  created_at
)

-- บันทึก/ประวัติ
client_notes (
  id, client_id, campaign_id, note, created_by, created_at
)

-- ไฟล์แนบ
attachments (
  id, campaign_id, file_name, file_url, file_type, uploaded_by, created_at
)
```

---

## 🔌 API Integrations

| Platform | API | ใช้ทำอะไร |
|----------|-----|----------|
| Facebook | Graph API / Page Insights | ดึง Reach, Engagement |
| YouTube | YouTube Data API v3 | ดึง Views, Watch Time |
| X (Twitter) | Twitter API v2 | ดึง Impressions, Engagement |
| TikTok | TikTok Business API | ดึง Views, Engagement |
| Instagram | Instagram Graph API | ดึง Reach, Engagement |
